# knowsy
Social Shopping App

Initial Set Up
- Add a config.js file with Twilio Authorization object:

    module.exports = {
      "token": string,
      "username": "string,
      "from": string,
      "to": string
    }

